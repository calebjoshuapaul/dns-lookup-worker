{
  "name": "dns-lookup-worker",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild ./index.js --bundle --platform=node --target=node16.8 --format=esm --outdir=dist --banner:js='import { createRequire as topLevelCreateRequire } from \"module\";\n const require = topLevelCreateRequire(import.meta.url);'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dns": "^0.2.2"
  },
  "devDependencies": {
    "esbuild": "^0.13.15"
  }
}
